# Denova 大范围变更回归清单

## 1. 文档定位

本清单用于指导 Denova 所有大范围功能变更、核心组件重构、跨模块调整和发布前回归。它是一份长期维护的测试目录，不绑定某个分支、版本或具体实现。

适用场景包括：

- 改动 Writing、Game、AgentChat 等核心用户流程。
- 改动被多个产品入口复用的 Agent runtime、会话、工具、上下文、工作区、导航、设置或 UI 组件。
- 改动 API、配置、Project 身份、持久化格式、版本恢复或用户内容目录。
- 大量文件或多个 package 同时变化，局部测试不足以证明整体行为。
- 准备合并重要变更，或准备正式发布版本。

使用原则：

- 第 5 节“基础不变量”在每次大范围变更中都要通过。
- 其余章节按“真实影响域”选择，不要求无脑执行全部条目。
- 影响域根据行为、依赖和数据流判断，不能只看直接修改了哪些文件。
- 改动共享底座时，必须回归所有主要消费者；例如共享 Chat 改动至少检查 Writing、Game 和 AgentChat。
- 未执行项要记录“不适用”的理由；外部依赖无法验证时要记录风险、负责人和补测条件，不能静默跳过。
- 测试结论只对被验证的源码树有效。后续代码、依赖、配置或冲突修复改变相关行为后，需要重跑受影响条目。

## 2. 使用流程

### 自动化测试的稳定性约束

- 浏览器用例统一从 `web/tests/support/fixtures.ts` 导入 `test`。共用 fixture 阻止未模拟的公网请求；外部响应通过页面级 route 显式模拟，应用运行所需的静态资源随包提供。失败诊断随 Playwright 报告上传。
- `createAndOpenBook` 每次调用生成独立书名与 Project，显示名断言使用返回的 `title`。不要用固定目录复用前一个用例的数据；`--repeat-each` 用于检验隔离性，不作为失败重试。
- 共用 fixture 在关闭页面后恢复语言、主题、runtime 默认值、模型配置和终端 shell，并核对恢复结果。涉及认证凭据等其他全局状态的用例仍需明确自己的隔离与清理策略；Project 设置由该用例独立的 Project 承载。
- 异步测试等待目标页面、接口回执、事件或最终状态，不使用固定 sleep。旅程总超时和单阶段等待预算分别设置；不让文件准备、fixture 初始化消耗后续异步阶段的预算。
- Go 测试使用取消与清理保证后台任务先结束、日志先关闭，再删除临时目录。竞争条件通过可控事件或事务冲突复现，不能只依赖概率重跑。
- CI 浏览器分片分别使用独立后端与数据目录，每片单 worker，保持零自动重试。最终 `Test and build` 必须汇总所有分片、Go/单测、构建与生产包冒烟；不能只看某个绿色分片。

### 回归执行步骤

1. 先阅读 Diff、调用链、配置和数据边界，填写第 3 节影响域。
2. 为每个选中域确定正常路径、失败路径、刷新/重启路径和数据保护路径。
3. 执行第 4 节自动化门禁和第 5 节基础不变量。
4. 执行所有选中功能域的检查项；共享能力按消费者矩阵分别验证。
5. 将结果保存为 `docs/tests/<change-name>-regression-<date>.md`，保留环境、证据、失败、修复和复验记录。
6. 如果测试中继续修改了实现，重新评估影响域，不只复验刚修复的单点。

## 3. 变更记录与影响域选择

### 执行记录

| 字段 | 值 |
| --- | --- |
| 变更名称 / PR |  |
| 基线提交 |  |
| 被测提交 |  |
| 变更目标与用户影响 |  |
| 涉及的 API / 配置 / 数据 |  |
| 选中的回归域 |  |
| 测试日期与负责人 |  |
| 操作系统 / 架构 |  |
| 浏览器与视口 |  |
| 语言 / 主题 |  |
| 模型与协议 |  |
| 测试数据副本 |  |
| 自动化结果 |  |
| 浏览器与日志证据 |  |
| 未覆盖项、原因与风险 |  |

### 影响域选择表

| 回归域 | 何时必须选择 |
| --- | --- |
| 基础不变量 | 所有大范围变更，始终选择 |
| 导航、布局与本地化 | 一级菜单、路由、面板、Tab、共享组件、主题、字体、响应式或用户文案变化 |
| Agent runtime 与会话 | Agent 生命周期、流式事件、消息投影、工具执行、权限、Ask、Goal、SubAgent、恢复或会话存储变化 |
| 模型上下文与缓存 | Prompt、Context State、Skills 目录、工具 schema、裁剪、cleanup、compaction、历史窗口或 trace 变化 |
| Writing | 书籍、章节、大纲、编辑器、搜索替换、评论、Writing Agent、Diff Review 或写作会话变化 |
| Game | 故事、回合、Actor State、Director、规则、事件、行动建议、历史、分支或游戏 Agent 变化 |
| AgentChat、Project、Files 与 Terminal | Project 注册表/身份、通用对话、工作台 Tab、后台 Book、文件树、Monaco、PTY 或 WebSocket 变化 |
| 设置、模型与安全 | Settings、Model Profile、协议适配器、密钥、权限规则、远程访问或用户/工作区作用域变化 |
| 资料、预设、Skills、自动化与图像 | 共用创作资源、CAS、配置管理工具、Skill 管理、Automation 或图像生成变化 |
| 数据、版本与恢复 | API 字段、文件结构、持久化真源、迁移/清理、版本 Diff、恢复、备份或并发写入变化 |
| 性能与长会话 | 列表、虚拟化、滚动、SSE、轮询、缓存、长历史、大文件、渲染或大对象变化 |
| 平台与发布 | 构建、内嵌资源、安装器、updater、跨平台代码或正式发版 |

常见联动关系：

- 共享消息列表、输入框或工具卡变化：选择“Agent runtime 与会话”，并在 Writing、Game、AgentChat 中分别验证。
- Project 身份或工作区事件变化：选择 Writing、Game、AgentChat/Files、数据恢复，检查前台与后台 Project 隔离。
- Context、Prompt 或 Tool Schema 变化：选择 Agent runtime、模型上下文与缓存，并覆盖所有使用该能力的 Agent 类型。
- 一级菜单或共享页面变化：选择导航域，同时从 Writing 和 Game 两个入口进入共享页面。
- 持久化、版本、删除或覆盖行为变化：选择数据恢复，并只在临时目录或完整副本上执行破坏性验证。
- API 或配置字段变化：同步选择前端消费者、后端服务、持久化和错误处理，不只验证一个请求。

## 4. 自动化与构建门禁

大范围变更按 AGENTS.md 选择必要检查，以下为 Bash 示例。Windows 原生使用 PowerShell 匹配的语法，在根目录与 `agent/` 分别执行 Go 检查，并使用当前平台支持的完整构建入口。

```bash
git diff --check

go test -count=1 ./...
(cd agent && go test -count=1 ./...)

go vet ./...
(cd agent && go vet ./...)

pnpm --dir web check:i18n
pnpm --dir web test
./scripts/build.sh
```

按风险追加：

- 并发、goroutine、Agent runtime、文件监听、持久化或异步生命周期变化：对相关 package 执行 `go test -race -count=1`；共享底座变化执行根模块与 `agent` 模块全量 race。
- 依赖、网络、安全、安装或正式发布变化：在两个 Go module 执行 `govulncheck`，并检查生产前端依赖。
- 前端布局或交互变化：运行相关组件测试，再用 Playwright 做可重复的系统验证。
- API、Agent 主流程、工具调用、上下文、版本恢复或工作区状态变化：优先增加或执行集成测试。

验收要求：

- [ ] 仅依赖变更在相关 module 执行 `go mod tidy`；清单核验可用 `go mod tidy -diff`。依赖文件只有预期变化，`git diff --check` 无错误。
- [ ] 所有相关测试、全量构建和静态检查通过；没有 race、panic、goroutine 泄漏或未处理 Promise rejection。
- [ ] 没有新增单项运行超过 3 秒的低效单元测试。
- [ ] 中英文 i18n key 对齐；用户界面按当前语言只展示中文或英文，不在同一交互中重复展示双语。
- [ ] 按 AGENTS.md 的宿主平台入口完成完整构建，包含主程序、updater、前端和内嵌资源、Skills、ripgrep；交叉编译不代替目标平台运行验证。

## 5. 基础不变量

以下条目在每次大范围变更中都必须通过：

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| BASE-01 | 启动当前源码并打开已有 Project | 应用可加载，当前 Project、基础设置和主界面正确恢复，无启动崩溃或持续失败请求 |
| BASE-02 | 打开 Writing，切换章节并完成一次编辑保存 | 正文、目录和保存链路可用；不会覆盖其他文件或 Project |
| BASE-03 | 打开 Game，进入已有故事并完成一次输入；允许使用当前默认模型配置 | 输入、流式输出、正文/状态提交和刷新恢复可用；不会产生半回合 |
| BASE-04 | 依次进入 Writing、Game 和共通能力一级页面 | 各入口直接进入对应目的地，不出现全局模式切换器；任何时候只有一个一级菜单高亮 |
| BASE-05 | 在两个 Project 间切换并分别读取内容 | API、缓存、会话、事件和写入按 Project 隔离，无跨 Project 内容污染 |
| BASE-06 | 刷新页面并检查最近操作 | 已提交内容、活动页面和必要布局状态正确恢复，不重复执行副作用 |
| BASE-07 | 检查浏览器控制台、网络请求和服务端日志 | 无未解释 exception、重复 4xx/5xx、错误轮询、panic 或敏感信息泄漏；错误按当前语言展示并保留 Log ID |
| BASE-08 | 检查所有涉及用户内容的删除、覆盖或自动调整 | 用户内容优先保护；破坏性行为有明确确认、备份或可回滚路径，不静默丢数据 |

## 6. 测试数据与浏览器环境

前端测试分层：

- `pnpm --dir web test:playwright`：主测试层，同一套 Playwright Test 同时承载浏览器行为回归与核心 E2E。
- `pnpm --dir web test:browser`：浏览器交互回归，重点验证导航、响应式布局、主题与双语行为。
- `pnpm --dir web test:e2e`：连接隔离的真实 Denova 后端与本地确定性模型，覆盖写作保存和游戏回合持久化等核心链路。
- 发布包回归：先解压当前宿主平台的产物，再运行 `DENOVA_E2E_PACKAGE_DIR=/absolute/path/to/denova pnpm --dir web exec playwright test --project=e2e`；复用隔离数据和确定性模型，直接检查包内二进制、前端及 Skills。不可与其他 E2E 同时运行。
- `pnpm --dir web test:unit`：辅助测试层，仅保留纯逻辑、解析器、状态转换和需要快速反馈的组件/Hook 测试。
- `pnpm --dir web test`：CI 与发布使用的完整前端测试入口，先跑 Vitest，再一次性跑全部 Playwright 项目。

Playwright 的后端数据固定写入 `web/test-results/runtime`，每次启动时重建；模型请求只发送到本地确定性服务。自动化测试不得复用开发者正在运行的 Denova 后端，也不得读取真实 `DENOVA_DIR`。

根据选中的影响域准备最小测试集，不必为无关变更创建所有数据：

- 全新的临时 `DENOVA_DIR`：用于首次启动、创建和安装类测试。
- 当前支持格式的已有用户数据副本：包含已完成会话、游戏故事、资料、预设和版本历史。
- 长篇 Book：包含大量章节、长正文、深层目录、长文件名和多份打开的文档。
- 普通目录 Project：与当前 Book 不同，用于 General Agent、Files 和 Terminal。
- 长会话：包含 thinking、多工具、大结果、SubAgent 和历史分页。
- 真实模型配置：协议或模型相关变化时，覆盖所有被改动的协议；图像变化时准备可删除的测试图片目录。

浏览器选择：

- 开发过程中用应用内 Browser 快速检查真实页面。
- 大型前端或核心组件改造，回归 Writing 和 Game 主链路；开发优先应用内浏览器，系统验证使用 Playwright。
- 需要稳定、可重复断言时使用 Playwright。
- 依赖用户真实账号、登录态或现有浏览器会话时使用 Chrome。
- 已有前端进程时直接复用热更新；不要 kill 或再启动一套。后端变更按 AGENTS.md 的当前宿主入口更新可见进程，只操作当前仓库的后端；Windows 整体重启脚本不代替仅重启后端。

涉及布局或用户交互时，至少检查：

- 宽屏与窄屏。
- light 与 dark。
- 中文与英文分别展示，界面跟随语言配置且不同时展示双语。
- 长文本、空数据、加载、失败和无权限状态。
- 键盘焦点、列表整行展开和关键操作可达性。

## 7. 导航、布局与本地化

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| NAV-01 | 从 Writing、Game 分别进入所有受影响的共通能力一级页面 | 直接进入对应目的地，不保留额外全局模式；只有当前一级菜单高亮 |
| NAV-02 | 拖动受影响的一级菜单、侧栏或分栏，折叠后刷新并切换 Project | 只有真实用户调整会持久化；折叠不被拖动误触发；不同页面和 Project 不串值 |
| NAV-03 | 在宽窄屏切换受影响页面、Dialog、Tab 和抽屉 | 自适应布局可用，无页面级横向溢出或被遮挡的主操作 |
| NAV-04 | 分别使用中文和英文检查新增文案、空状态、错误和提示 | 文案跟随语言配置，不在同屏重复展示双语，不出现缺失 key 或写死单一语言 |
| NAV-05 | 用键盘遍历一级菜单、列表和主要 Dialog | 焦点可见且顺序合理；当前目的地明确；列表点击父项整行即可展开/折叠 |

## 8. Agent runtime 与会话

共享 runtime 变化时，先选择所有实际消费者，再在每个入口至少完成一个真实 Run：Writing Agent、Game Agent、AgentChat、Automation、Image Agent，以及受影响的 SubAgent。Native 与外部 runtime 分别核验实际能力；游戏不得开启 Goal。

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| AGENT-01 | 观察 thinking、正文、工具名、参数增量、进度和结果 | SSE 实时到达；一次工具调用只对应一张卡；事件顺序、状态和最终正文正确 |
| AGENT-02 | 在受影响权限模式下执行无害的只读和测试写入 | 权限按会话固定；审批只由真实工具输入和规则决定，展示文案不参与安全判断 |
| AGENT-03 | Run 进行中刷新页面 | 同一后端进程内可附着活动 Run；不会创建重复 Run、重复消息或重复工具副作用 |
| AGENT-04 | Run 中输入追问，再分别测试发送和中断 | 发送与中断作用于准确会话和 Run；排队顺序稳定，不误伤其他会话 |
| AGENT-05 | 如果改动恢复语义，在工具前后分别重启后端并重试 | UI 明确表达终态；不会静默重放非幂等副作用；已完成内容和 canonical 数据仍在 |
| AGENT-06 | 创建含长 thinking、多工具和长结果的单个回合，刷新并向上分页 | 历史按完整用户回合展示，不截断末尾工具或正文，不错误提示还有更早消息 |
| AGENT-07 | 回答、取消并重复提交 Ask；必要时冷重启 | 只有一个规范结果；重复请求幂等；未知或失效交互给出明确错误 |
| AGENT-08 | 执行 read/glob/grep、Shell、edit/delete 等受影响工具 | schema、分页、计数、路径边界、部分成功和错误反馈正确；写入进入可审阅变更链路 |
| AGENT-09 | 如果改动 Goal/Plan，在支持的入口测试创建、编辑、暂停、清除和续跑 | 状态和归属正确，跨刷新恢复；游戏 UI、API 与执行层拒绝 Goal；外部 Plan 只保存已确认快照 |
| AGENT-10 | 运行有长输出的 SubAgent，分别在运行中和完成后打开详情 | 运行中跟随最新输出；完成后可从开头阅读并跳到底部；委派边界与来源稳定 |
| AGENT-11 | 测试短回复、长回复和用户主动上翻 | 短会话无大块空白；长会话默认到最新；主动上翻后不被强制拉回 |

## 9. 模型上下文与缓存

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| CTX-01 | 对比连续两轮无状态变化与一轮有状态变化的最终模型请求 | 稳定前缀保持不变；无变化不重复注入；变化只旋转必要边界，缓存命中不被无关 UI/元数据破坏 |
| CTX-02 | 检查所有新增模型可见片段 | 使用英文、产品中立；有明确 source、purpose、revision/identity；硬上限可配置且通常高于 50 KiB，不无提示截断 |
| CTX-03 | 修改 Project 指令、动态状态、Skill 或工具描述中的一个来源 | 只有对应上下文边界变化；其他稳定片段、工具 schema 和缓存前缀保持稳定 |
| CTX-04 | 触发受影响的 cleanup 或 compaction 路径并继续多轮对话 | 用户输入、assistant 正文、工具协议和必要状态不丢失；不会循环压缩、重写 canonical 历史或破坏后续调用 |
| CTX-05 | 产生超大可恢复工具结果并继续对话 | 完整结果安全保存；模型获得有界且可恢复的预览/receipt；UTF-8、Markdown 和来源标记完整 |
| CTX-06 | 使用超过 resident window 的长会话重启并继续 | 展示历史和模型上下文各自正确；最新输入与工具结果不丢；能从规范存储读取所需区间 |
| CTX-07 | 使用开发者模式或 trace 检查最终请求 | system、messages、tools、cache usage 与分叉点可审计；开发者模式关闭时不额外持久化完整敏感内容 |

## 10. Writing

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| WRITE-01 | 新建或打开 Book，必要时导入 `.txt` / `.md` | 目录、章节、正文和统计正确；失败不留下半成品 |
| WRITE-02 | 在长篇作品展开/折叠卷、定位章节、切换多文档与 Tab | 长列表可用；每个文档恢复自己的阅读位置，Tab 顺序和编辑状态稳定 |
| WRITE-03 | 编辑正文并测试自动保存、外部修改、冲突和文件删除 | 内容/结构事件区分正确；无自刷新回声；冲突可恢复，删除文件不恢复为空白 Tab |
| WRITE-04 | 执行普通/正则搜索与替换 | 预览、命中数和实际文件一致；不扫描 Denova 状态目录；失败不产生不可恢复的部分写入 |
| WRITE-05 | 让 Writing Agent 新建、修改和删除测试文件 | 写入绑定正确 Project；变更摘要及时出现；完成后 Diff、文件状态和磁盘内容一致 |
| WRITE-06 | 在 Diff Review 筛选文件、切换文件、评论、接受/驳回、Undo/Redo | 编辑器状态稳定；新增/修改/删除/冲突正确；每个操作可逆且刷新后一致 |
| WRITE-07 | 在正文或 Lore 选区创建评论并提交给 Agent | 锚点和 revision 正确；反馈只消费一次；刷新或重连不重复出现 |
| WRITE-08 | 测试受影响的续写、章节确认、状态更新和会话 CRUD | 目标章节和状态边界正确；会话不串 Project，消息数和历史实时更新 |

## 11. Game

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| GAME-01 | 新建故事并完成开局 | 空故事可安全打开；正文、行动建议、状态和首回合原子落盘 |
| GAME-02 | 连续完成至少三个回合，覆盖建议输入、自由输入、需要规则和无需规则路径 | 叙事连续；每回合正文、状态、建议和真实回合数一致；同一判定不会意外重复执行 |
| GAME-03 | 覆盖正文与工具调用分布在多条 assistant 消息的完成路径 | 最终可见正文、状态与提交结果完整，不因最后一条消息为空或仅含工具而丢失 |
| GAME-04 | 中断一个生成中的回合并重试，再重新生成已完成回合 | 中断不留下半回合；重试只提交一次；重新生成替换目标回合而不错误增加计数 |
| GAME-05 | 编辑已保存 AI 回复并刷新 | 修改稳定保存，不破坏状态、建议、分支和后续历史 |
| GAME-06 | 从历史回复创建分支，切换并删除测试分支 | 来源、自动切换、路线图和真实回合数正确；主线内容不变 |
| GAME-07 | 加载更早历史、搜索事实并跳转旧回合 | 分页不丢回合；搜索来源正确；跳转到真实历史位置而非最新回合 |
| GAME-08 | 检查 Director、Actor State、事件、规则和受影响模块 | 计划与最新回合一致；并发保存不互相覆盖；模块关闭和配置作用域正确 |
| GAME-09 | 刷新、切换故事并在 ready 状态观察网络与性能 | 不重复提交或串故事；空闲时无不必要的大对象轮询或持续错误请求 |

## 12. AgentChat、Project、Files 与 Terminal

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| WORKBENCH-01 | 添加 Book 和普通目录 Project，测试重命名、排序、归档和目录重连 | Project ID 稳定；历史和状态保留；管理操作不意外跳转当前页面 |
| WORKBENCH-02 | 新建、切换、重命名和删除 AgentChat 会话 | 会话绑定正确 Project；列表、消息数、草稿和历史跨刷新正确 |
| WORKBENCH-03 | 创建多个对话/终端/阅读器 Tab，拖动排序并跨左右工作区迁移 | Tab、草稿、后台 Run 和编辑器保持稳定；分栏状态正确恢复 |
| WORKBENCH-04 | 在 Files 创建多级路径、保存、重命名、拖动、粘贴和删除 | 目标目录、选择恢复、部分成功和错误回执正确；无错误自动保存或跨 Project 写入 |
| WORKBENCH-05 | 模拟外部文件内容与目录结构变化 | 内容变化只刷新内容，结构变化才刷新树；本地操作不产生事件回声 |
| WORKBENCH-06 | 在后台 Book 打开 Reader/Lore 并编辑 | 所有资源绑定后台 Project；前台 Book 和活动页面不被替换 |
| WORKBENCH-07 | 启动 Shell 或已配置 CLI，测试输入输出、resize、主题、退出与重连 | PTY 可用；CLI 退出回到原目录 Shell；重连恢复有界 scrollback，不留下假活连接 |

## 13. 设置、模型与安全

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| SETTINGS-01 | 编辑用户级与工作区级设置，再切换 Project | 默认值、作用域和持久化位置清晰；用户设置、工作区覆盖和运行时状态不混写 |
| SETTINGS-02 | 修改 Model Profile，切换 provider/protocol/Base URL 并测试连接 | 自定义模型可用；密钥/Header 只在同源继承；协议选项不串线；未保存草稿按设计参与测试 |
| SETTINGS-03 | 如果改动模型协议，用真实调用覆盖所有受影响协议 | 请求序列化、thinking、工具续传、usage、错误解析和后续轮次正确 |
| SETTINGS-04 | 修改全局模型/权限后打开旧会话和新会话 | 已有会话保持自己的配置，新会话按规则继承；分支复制和作用域正确 |
| SETTINGS-05 | 分别使用中文/英文、light/dark、字体和动效设置 | UI 跟随配置且只显示当前语言；主题为纯黑/纯白基调，已有页面同步更新 |
| SETTINGS-06 | 如果改动远程访问或安全边界，测试认证、LAN、路径和审批规则 | 未授权请求被拒绝；外部写入、网络和高危操作需要正确授权；日志不泄漏凭据 |

## 14. 资料、预设、Skills、自动化与图像

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| RESOURCE-01 | 在 Writing/Game 读取并编辑受影响的 Lore 或预设 | 共用资源即时同步；revision/CAS 生效；冲突不覆盖其他保存；模块作用域正确 |
| RESOURCE-02 | 创建、编辑、排序和删除受影响资源 | 整行可展开；排序与自动保存稳定；删除有确认且不会留下失效引用 |
| RESOURCE-03 | 创建/编辑 Skill，并通过命令选择器显式调用 | 候选、键盘和 token 正确；完整 Skill 只按需加载；失败有明确回退 |
| RESOURCE-04 | 检查 Project 指令、Skill 目录和配置管理工具 | 模型可见内容为英文且来源明确；配置管理工具受当前作用域和 revision 约束 |
| RESOURCE-05 | 创建 Automation，手动运行、确认、取消并检查重启后的状态 | 定义和 Run 按 Project 隔离；确认幂等；不会永久卡在 running 或重复执行副作用 |
| RESOURCE-06 | 生成单图与多图，并制造一个逐项失败 | 已成功图片继续返回并落盘；逐项失败明确；只有全部失败时整体失败；路径绑定正确 Project |
| RESOURCE-07 | 如果改动 Trajectory 或 Agents Project，检查开关、详情、导出、Profile 编辑、版本保存和恢复 | 敏感数据只在启用时记录；Trajectory 只读且可审计；Profile 变更可通过 Project Versions 检查和恢复 |

## 15. 数据、版本与恢复

所有破坏性测试只在临时目录或完整副本执行。

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| DATA-01 | 用全新数据与当前支持格式的已有数据分别启动 | 当前数据契约可用；自动修改用户内容前有明确备份或可回滚路径 |
| DATA-02 | 在多个 Book/Project 间切换，同时保留草稿、自动保存和后台会话 | 切换前正确保存或明确阻止；返回后草稿、会话和布局属于原 Project |
| DATA-03 | 放入损坏的索引、会话或可重建投影 | 单个坏项被隔离；规范数据不被无声覆盖；其他内容和新建操作仍可用 |
| DATA-04 | 如果变更 API/配置/文件结构，搜索并验证所有前后端调用点 | 入参出参命名一致；旧路径被直接移除而不是保留 fallback；不兼容行为写入 Changelog |
| DATA-05 | 创建手动版本，修改多个文件后查看 Diff | 版本元数据、文件列表和完整 Diff 正确；后台 Project 不污染前台 Book |
| DATA-06 | 查看整工作区恢复计划并执行恢复 | UI 明确告知备份；恢复到目标内容，并可回到恢复前状态 |
| DATA-07 | 只恢复新增、修改和删除三类路径，并注入中途失败 | 全部路径先预检；失败时整体回滚；不出现混合版本；路径逃逸和符号链接被拒绝 |
| DATA-08 | 在未保存编辑或未审阅 Agent Diff 存在时发起恢复/切换 | 明确阻止或要求确认，不静默覆盖用户内容 |

## 16. 性能与长会话

| ID | 操作 | 通过标准 |
| --- | --- | --- |
| PERF-01 | 在长章节、长目录和深层文件树中切换、搜索和滚动 | 本地交互不因模型等待之外的工作阻塞；虚拟化、选中和定位正确 |
| PERF-02 | 运行包含高速 thinking、正文 delta 和多工具事件的长 Run | 页面保持可交互；批处理不丢事件或改变顺序；工具和完成边界及时显示 |
| PERF-03 | 打开长历史并多次向上分页，再切换 Tab/Project 返回 | 内存、DOM 和请求数量保持有界；滚动位置与底部锁状态正确 |
| PERF-04 | 在空闲 Writing、Game、AgentChat 页面观察网络 | 无重复 Skills/Settings/完整 snapshot 请求，无持续失败重试或无主任务轮询 |
| PERF-05 | 检查构建输出与新增依赖 | 无异常首屏或 chunk 体积增长；新增依赖确实降低复杂度并按需加载 |

## 17. 平台与正式发布附加项

只有涉及跨平台、构建、安装、更新或正式发布时才执行本节。

版本元数据：

- [ ] `web/package.json` 使用发布版本 `X.Y.Z`。
- [ ] `CHANGELOG.md` 存在 `## [vX.Y.Z]`，说明用户可感知变化、修复和不兼容行为。
- [ ] `README.md` 与 `README.en.md` 的版本、能力和安装说明同步。
- [ ] Release commit、PR title 和 Tag 符合约定，Tag 为 `vX.Y.Z`。

构建与产物：

```bash
scripts/build-github-release.sh "vX.Y.Z"
```

- [ ] 生成 macOS/Linux ARM64、x64 与 Windows x64 产物和 `checksums.txt`。
- [ ] 每个归档包含主程序、updater、Web、Skills、工具、许可、配置和发布文档。
- [ ] 所有 SHA-256 校验通过；文件名、内部目录和 Tag 一致。
- [ ] 在当前原生平台运行主程序与 updater 的 `--version`，均输出 `X.Y.Z`。
- [ ] 用临时安装目录和数据目录完成指定版本安装、重复安装和失败回滚。
- [ ] 从原生归档完成“启动 → 新建 Book → 一轮 Writing → 一轮 Game → 重启恢复”冒烟。
- [ ] Git tag 指向已测试提交；Release notes 只写简要用户变化；上传资产与本地校验一致。

## 18. 最终结论

放行前必须满足：

- [ ] 第 4 节自动化与构建门禁通过。
- [ ] 第 5 节基础不变量全部通过。
- [ ] 第 3 节选中的所有影响域均完成，未执行项有明确理由和风险记录。
- [ ] 浏览器、网络和服务端日志中没有未解释的高风险错误。
- [ ] 所有临时数据已清理；破坏性验证的目标、备份和恢复位置已记录。
- [ ] 如果是正式发布，第 17 节全部通过。

以下问题直接阻塞合并或发布：用户内容丢失或跨 Project 污染、一级导航目的地或高亮状态错误、Writing 或 Game 核心流程不可用、Agent 重复执行非幂等副作用、恢复流程不可回滚、模型上下文协议断链、构建/测试/race/安全检查失败，或发布包无法安装启动。
