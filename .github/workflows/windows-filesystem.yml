name: Windows filesystem durability

on:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/windows-filesystem.yml"
      - "go.mod"
      - "go.sum"
      - "internal/localfs/**"
      - "internal/portablepath/**"
      - "internal/project/**"
      - "internal/app/**"
      - "config/**"
      - "internal/workspace/documentreview/**"
      - "internal/workspace/change/**"
      - "agent/go.mod"
      - "agent/go.sum"
      - "agent/tools/**"
  pull_request:
    paths:
      - ".github/workflows/windows-filesystem.yml"
      - "go.mod"
      - "go.sum"
      - "internal/localfs/**"
      - "internal/portablepath/**"
      - "internal/project/**"
      - "internal/app/**"
      - "config/**"
      - "internal/workspace/documentreview/**"
      - "internal/workspace/change/**"
      - "agent/go.mod"
      - "agent/go.sum"
      - "agent/tools/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Windows filesystem durability regressions
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: |
            go.sum
            agent/go.sum

      - name: Verify shared directory durability consumers
        run: go test ./internal/localfs ./internal/workspace/documentreview ./internal/workspace/change -count=1

      - name: Verify Project paths and released-data migration
        run: |
          go test ./internal/project ./internal/portablepath -count=1
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          go test ./internal/app -run "TestManagedDataDirectoryRunsAfterCopyingToAnotherRoot|TestReleasedV033WritingSessionMigratesAndContinues|TestPortableDataMigration" -count=1

      - name: Verify Agent workspace filesystem boundaries
        working-directory: agent
        run: go test ./tools -count=1
